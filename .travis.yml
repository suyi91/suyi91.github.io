# .travis.yml
language: node_js
node_js:
  - '10'
branches:
  only:
    - sources
script:
  - git config user.name "${GH_NAME}"
  - git config user.email "${GH_EMAIL}"
  - npm install && npm run build && npm run publish
